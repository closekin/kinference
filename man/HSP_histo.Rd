% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinference.R
\name{HSP_histo}
\alias{HSP_histo}
\title{HSP_histo(): PLOD distro frequency plot for the HSP and/or HTP bump regions}
\usage{
HSP_histo(hsps, lb, ub = max(hsps$PLOD) + 10, fullsib_cut, bin = 5,
  HSPmean = TRUE, HSPdist = TRUE, ...)
}
\arguments{
\item{hsps}{the output of a call to \code{find_HSPs()}}

\item{lb}{PLOD lower bound for plot extent. Should exclude the UP bump}

\item{ub}{PLOD upper bound for plot extent. Defaults to maximum PLOD score plus a
little padding}

\item{fullsib_cut}{PLOD score above which there are only full-sibs}

\item{bin}{hist bin width. Default 5. lb, ub, and bin together define 'breaks',
so you can't pass 'breaks' via \code{...}}

\item{HSPmean}{plot the mean PLOD for HSPs? Default TRUE}

\item{HSPdist}{plot the distribution of PLOD for HSPs? Default TRUE}

\item{...}{additional pars, passed to \code{hist()}}
}
\description{
Plots an absolute-frequency histogram for the output of \code{find_HSPs()}, with
the lower bound set by the user. Lower bounds should be set to exclude (as much
as possible) the UP bump, as this will otherwise swamp the signal from the HSP
bump. Users must manually set a lower bound for full-sibling PLODs ('fullsib_cut')
on order to exclude full-siblings from the variance estimate for HSP PLODs.
}
\seealso{
PLOD_loghisto
}
